# 빌드 JVM 인코딩을 UTF-8로 고정(OS 기본 charset에 의존하지 않도록).
# 참고: 프로젝트 경로에 한글이 있으면(예: '바탕 화면') Gradle 테스트 워커의
# 클래스패스가 깨져 테스트가 ClassNotFound로 실패하므로 ASCII 경로에서 작업한다.
org.gradle.jvmargs=-Dfile.encoding=UTF-8 -Dsun.jnu.encoding=UTF-8
